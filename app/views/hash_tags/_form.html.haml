= form_for @hash_tag do |f|
  - if @hash_tag.errors.any?
    #error_explanation
      %h2= "#{pluralize(@hash_tag.errors.count, "error")} prohibited this hash_tag from being saved:"
      %ul
        - @hash_tag.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
